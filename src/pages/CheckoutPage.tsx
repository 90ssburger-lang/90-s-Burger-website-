import { useEffect, useState } from 'react';
import { Link, Navigate } from 'react-router-dom';
import { Check, CreditCard, Wallet } from 'lucide-react';
import { MainLayout } from '@/components/layout/MainLayout';
import { useCart } from '@/contexts/CartContext';
import { useCreateOrder } from '@/hooks/useOrders';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Textarea } from '@/components/ui/textarea';
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group';
import { formatCurrency, SHIPPING_COST } from '@/lib/utils';
import { trackMetaEvent } from '@/lib/analytics';
import { toast } from 'sonner';

export default function CheckoutPage(){
 const {items,subtotal,discount,appliedCoupon}=useCart();const createOrder=useCreateOrder();const [method,setMethod]=useState<'cod'|'card'>('cod');const [orderId,setOrderId]=useState('');const [form,setForm]=useState({name:'',phone:'',address:'',notes:''});const delivery=SHIPPING_COST;const total=Math.max(0,subtotal+delivery-discount);
 useEffect(()=>{if(items.length)trackMetaEvent('InitiateCheckout',{content_ids:items.map(i=>i.product.id),num_items:items.reduce((s,i)=>s+i.quantity,0),value:total,currency:'EGP'})},[]);
 const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!form.name||!form.phone||!form.address){toast.error('Please enter your name, phone and address');return}try{
   if(method==='cod'){const order=await createOrder.mutateAsync({shippingAddress:{firstName:form.name,lastName:'',address1:form.address,city:'Cairo',state:'Cairo',zip:'',country:'EG',phone:form.phone},customerName:form.name,customerEmail:'guest@90sburger.local',notes:form.notes,paymentMethod:'cod',shippingCost:delivery,couponCode:appliedCoupon?.code||null});setOrderId(order.id);trackMetaEvent('Purchase',{content_ids:items.map(i=>i.product.id),value:total,currency:'EGP',order_id:order.id});return}
   const id=crypto.randomUUID();localStorage.setItem('90s_burger_pending_payment',JSON.stringify({orderId:id,customerEmail:'guest@90sburger.local',customerName:form.name,shippingAddress:{firstName:form.name,lastName:'',address1:form.address,city:'Cairo',state:'Cairo',zip:'',country:'EG',phone:form.phone},billingAddress:null,shippingCost:delivery,couponCode:appliedCoupon?.code||null,notes:form.notes,items:items.map(i=>({product_id:i.product.id,quantity:i.quantity}))}));
   const r=await fetch('/api/paymob/create-payment',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({merchantOrderId:id,billingData:{first_name:form.name,last_name:'Guest',email:'guest@90sburger.local',phone_number:form.phone,city:'Cairo',state:'Cairo',street:form.address,postal_code:'NA',country:'EG',apartment:'NA',floor:'NA',building:'NA',shipping_method:'NA'},items:items.map(i=>({product_id:i.product.id,quantity:i.quantity})),shippingCost:delivery,couponCode:appliedCoupon?.code||null,orderPayload:{userId:null,customerEmail:'guest@90sburger.local',customerName:form.name,notes:form.notes,shippingAddress:{firstName:form.name,address1:form.address,phone:form.phone,city:'Cairo',country:'EG'}}})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Payment setup failed');location.href=data.iframeUrl;
 }catch(err){toast.error(err instanceof Error?err.message:'Could not place order')}};
 if(orderId)return <MainLayout><div className="container mx-auto px-4 py-20"><div className="mx-auto max-w-lg rounded-3xl border bg-white p-8 text-center shadow-xl"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-green-100"><Check className="h-10 w-10 text-green-600"/></div><h1 className="mt-6 text-4xl font-black uppercase">Order confirmed!</h1><p className="mt-3 text-muted-foreground">We're firing up the grill. Keep this order ID for your reference.</p><div className="mt-6 rounded-2xl bg-[#fff7df] p-4"><small className="font-bold uppercase text-muted-foreground">Order ID</small><p className="mt-1 break-all font-mono font-bold">{orderId}</p></div><Link to="/shop"><Button className="mt-7 rounded-full bg-[#ef3e2f] px-8 text-white">Back to menu</Button></Link></div></div></MainLayout>;
 if(!items.length)return <Navigate to="/cart" replace/>;
 return <MainLayout showFooter={false}><div className="container mx-auto px-4 py-8"><Link to="/cart" className="text-sm font-bold">← Back to cart</Link><h1 className="mt-5 text-4xl font-black uppercase">Fast checkout</h1><p className="mt-2 text-muted-foreground">No account needed. Just tell us where to bring it.</p><form onSubmit={submit} className="mt-8 grid gap-8 lg:grid-cols-[1fr_380px]"><div className="space-y-6 rounded-3xl border bg-white p-5 sm:p-7"><div><Label htmlFor="name">Name</Label><Input id="name" required className="mt-2 h-12" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><Label htmlFor="phone">Phone</Label><Input id="phone" type="tel" required className="mt-2 h-12" placeholder="01xxxxxxxxx" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><div><Label htmlFor="address">Delivery address</Label><Textarea id="address" required className="mt-2" placeholder="Area, street, building, floor and apartment" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></div><div><Label htmlFor="notes">Delivery notes</Label><Textarea id="notes" className="mt-2" placeholder="Landmark, gate instructions, allergies…" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></div><div><Label>Payment method</Label><RadioGroup value={method} onValueChange={v=>setMethod(v as 'cod'|'card')} className="mt-2 grid gap-3 sm:grid-cols-2"><label className="flex cursor-pointer items-center gap-3 rounded-2xl border p-4"><RadioGroupItem value="cod"/><Wallet/><span><strong className="block">Cash on delivery</strong><small className="text-muted-foreground">Pay at your door</small></span></label><label className="flex cursor-pointer items-center gap-3 rounded-2xl border p-4"><RadioGroupItem value="card"/><CreditCard/><span><strong className="block">Card</strong><small className="text-muted-foreground">Secure online payment</small></span></label></RadioGroup></div></div><aside className="h-fit rounded-3xl bg-[#17120f] p-6 text-[#fff7df] lg:sticky lg:top-24"><h2 className="text-2xl font-black uppercase">Your order</h2><div className="mt-5 max-h-64 space-y-3 overflow-auto">{items.map((i,n)=><div key={`${i.product.id}-${n}`} className="flex justify-between gap-3 text-sm"><span>{i.quantity}× {i.product.name}</span><strong>{formatCurrency(i.product.price*i.quantity)}</strong></div>)}</div><div className="mt-5 space-y-2 border-t border-white/15 pt-5 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{formatCurrency(subtotal)}</span></div><div className="flex justify-between"><span>Delivery</span><span>{formatCurrency(delivery)}</span></div>{discount>0&&<div className="flex justify-between text-[#f6b73c]"><span>Discount</span><span>-{formatCurrency(discount)}</span></div>}<div className="flex justify-between pt-3 text-xl font-black"><span>Total</span><span>{formatCurrency(total)}</span></div></div><Button type="submit" disabled={createOrder.isPending} className="mt-6 h-14 w-full rounded-full bg-[#ef3e2f] text-base text-white hover:bg-[#d92d20]">{createOrder.isPending?'Placing order…':method==='cod'?'Place order':'Continue to payment'}</Button></aside></form></div></MainLayout>;
}
